1 - As estruturas de repetição podem ser classificadas em:

Repetição com teste no início (while), Repetição com teste no final  (do - while), Repetição contada (for)

2 - Sobre arrays, marque a alternativa correta.


Permanecem com o mesmo tamanho em que foram criados

3 - String[] nomes = {"Camila", "Venilton", "Leonardo", "Renan", "Rafael"};
System.out.print(nomes.length);


Dado o código acima, o que será impresso no console?

5

4 - int i, num = 5;
        for (i = 0; i < 3; i++) {
            num += i;
            System.out.print(i);
        }

Dado o código acima, o que será impresso no console?

012

5 - “É um objeto utilizado para armazenar sequencialmente dados do mesmo tipo.”
Acima definimos:

Array

6 - Como podemos definir “Estruturas de repetição”:

Uma estrutura que permite a execução de uma sequência repetidamente, caso determinadas condições sejam
satisfeitas

7 - int num = 5, count = 1;
        do {
            num += count;  //(num = numero + count)//
            System.out.println(num);
        } while (count <= 3);

Dado o código acima, o que será impresso no console?

looping infinito

8 - int[][] numeros = {{4, 9, 8, 5}, {3, 0, 1, 5}, {1, 2, 7, 4}};
System.out.print(numeros[2][2]);

Dado o código acima, o que será impresso no console?

7

9 - int num = 5, count = 1;
        while(count <= 3) {
            ++count;
            num += count; (num = num + count)
        }
        System.out.println(num);

Dado o código acima, o que será impresso no console?

14

10 - Sobre Estruturas de repetição, julgue a alternativa INCORRETA:

As estruturas de repetição podem ser classificadas em: "while", "do-while" e "array".
